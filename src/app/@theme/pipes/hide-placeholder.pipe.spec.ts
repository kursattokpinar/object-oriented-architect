import { HidePlaceholderPipe } from './hide-placeholder.pipe';

describe('HidePlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new HidePlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
